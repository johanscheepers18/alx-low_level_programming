#include "main.h"

/**
 * _isdigit - used to check if c is a digit
 * @c: variable used to store input
 * Return: returns 1 if digit, 0 if not
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
