#include "main.h"

/**
 * _abs - Entry point
 * @x: used as the number to get the absolute value
 * Return: returns the absolute value of x
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
