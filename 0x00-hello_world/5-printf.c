#include <stdio.h>

/**
 * main - Entry point
 * Description this program is used to printa defined string
 * Return: Always 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");

	return (0);
}
