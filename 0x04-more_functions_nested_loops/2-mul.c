#include "main.h"

/**
 * mul - used to multiply two numbers
 * @a: first number
 * @b: second number
 * Return: returns the value of c, which is a multiplied by b
 */
int mul(int a, int b)
{
	int c;

	c = a * b;

	return (c);
}
