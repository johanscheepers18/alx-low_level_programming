#include "main.h"

/**
 * _isupper - Checks if c is uppercase
 * @c: variable used to store the character
 * Return: if c is uppercase return 1, else return 0
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
